# coding: utf-8

"""
    Subscriptions

    A subscription enables you to charge customers at specified intervals for a plan of their choice.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class Plan(BaseModel):
    """
    Plan object for which the subscription is to be created. This contains <code>plan_code</code>, <code>name</code>, <code>price</code>, <code>quantity</code>.
    """ # noqa: E501
    plan_code: Optional[StrictStr] = Field(default=None, description="Plan code of the plan that is to be subscribed to the customer.")
    name: Optional[StrictStr] = Field(default=None, description="Name generated by concatenation of the product name and the selected plan.")
    quantity: Optional[StrictInt] = Field(default=None, description="Required quantity of the chosen plan.")
    price: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Price of a plan for a particular subscription. If a value is provided here, the plan’s price for this subscription will be changed to the given value. If no value is provided, the plan’s price would be the same as what it was when it was created.")
    discount: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Discount amount applied for the plan.")
    total: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Total amount for the plan.")
    setup_fee: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Setup fee for the plan.")
    plan_description: Optional[StrictStr] = Field(default=None, description="Description of the plan exclusive to this subscription. This will be displayed in place of the plan name in invoices generated for this subscription.")
    tax_id: Optional[StrictStr] = Field(default=None, description="Unique ID of Tax or Tax Group that must be associated with the plan. <code>tax_id</code> must be empty for applying tax Exemption.")
    trial_days: Optional[StrictInt] = Field(default=None, description="Number of free trial days granted to a customer subscribed to this plan. Trial days for the subscription mentioned here will override the number of trial days provided at the time plan creation. This will be applicable even if exclude_trial=true.")
    __properties: ClassVar[List[str]] = ["plan_code", "name", "quantity", "price", "discount", "total", "setup_fee", "plan_description", "tax_id", "trial_days"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Plan from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Plan from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "plan_code": obj.get("plan_code"),
            "name": obj.get("name"),
            "quantity": obj.get("quantity"),
            "price": obj.get("price"),
            "discount": obj.get("discount"),
            "total": obj.get("total"),
            "setup_fee": obj.get("setup_fee"),
            "plan_description": obj.get("plan_description"),
            "tax_id": obj.get("tax_id"),
            "trial_days": obj.get("trial_days")
        })
        return _obj


